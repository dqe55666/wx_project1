<scroll-view class="scrollarea" scroll-y>
  <view class="page">
    <view class="hero">
      <text class="eyebrow">护理陪护服务</text>
      <text class="title">主页</text>
      <text class="subtitle">预约、记录、知识与心情漂瓶，都从这里开始。</text>
    </view>

<<<<<<< HEAD
    <view wx:if="{{currentAddress}}" class="location-strip">
      <text class="location-label">当前位置</text>
      <text class="location-address">{{currentAddress}}</text>
    </view>

    <view class="quick-grid">
      <view class="quick-item" bindtap="goCreateVisit">
        <view class="qi-icon" style="background:linear-gradient(135deg,#ffe4ec,#f8bbd0);">🩺</view>
        <text class="qi-name">新建陪诊</text>
      </view>
      <view class="quick-item" bindtap="goVisitList">
        <view class="qi-icon" style="background:linear-gradient(135deg,#fff3e0,#ffe0b2);">📋</view>
        <text class="qi-name">陪诊中</text>
      </view>
      <view class="quick-item" bindtap="goMall">
        <view class="qi-icon" style="background:linear-gradient(135deg,#fce4ec,#f48fb1);">🛍️</view>
        <text class="qi-name">护理商城</text>
      </view>
      <view class="quick-item" bindtap="goMessage">
        <view class="qi-icon" style="background:linear-gradient(135deg,#e1f5fe,#81d4fa);">💬</view>
        <text class="qi-name">消息</text>
      </view>
    </view>

    <view class="panel">
      <view class="section-title">医院单位</view>
      <picker mode="selector" range="{{hospitals}}" range-key="name" value="{{selectedHospitalIndex}}" bindchange="onHospitalChange">
        <view class="picker-field">
          <block wx:if="{{hospitals.length}}">
            <text>{{hospitals[selectedHospitalIndex].name}}</text>
            <text class="muted">{{hospitals[selectedHospitalIndex].displayDistance}}</text>
          </block>
          <text wx:elif="{{hospitalLoading}}" class="muted">医院单位加载中</text>
          <text wx:else class="muted">暂无可选医院单位</text>
=======
    <view class="entry-grid">
      <view wx:for="{{entries}}" wx:key="title" class="entry-card {{item.tone}}" data-url="{{item.url}}" bindtap="openEntry" hover-class="entry-card-active">
        <view class="entry-mark">{{index + 1}}</view>
        <view class="entry-content">
          <text class="entry-title">{{item.title}}</text>
          <text class="entry-desc">{{item.desc}}</text>
>>>>>>> 356664a594e3c96cd7bbca97cde122c4c2859d3d
        </view>
        <text class="entry-arrow">›</text>
      </view>
    </view>
  </view>
</scroll-view>
